  private static class Transaction {
    long timestamp;
    String action;
    double amount;
    String executor;
    String reason = "";

    Transaction(long timestamp, String action, double amount, String executor, String reason) {
    this.timestamp = timestamp;
    this.action = action;
    this.amount = amount;
    this.executor = executor;
    this.reason = reason;
    }
  }